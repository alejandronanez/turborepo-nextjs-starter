{
  "name": "@alejandronanez/monorepo-starter",
  "description": "A monorepo that uses Next.js, Tailwind, TypeScript, Jest and Storybook",
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "^1.5.5"
  },
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache",
    "build": "turbo run build",
    "test": "turbo run test"
  }
}
